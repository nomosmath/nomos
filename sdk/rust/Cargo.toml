[package]
name = "nomos-sdk"
version = "0.5.0"
edition = "2024"

[dependencies]
nomos-core = { path = "../.." }
serde = { version = "1", features = ["derive"] }
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1.38", features = ["rt"] }
