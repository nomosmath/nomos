[project]
name = "nomos-sdk"
version = "0.5.0"
description = "Python SDK for Nomos proof verification protocol"
license = "MIT OR Apache-2.0"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest", "httpx"]
