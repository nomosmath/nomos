[package]
name = "nomos-cli"
version = "0.5.0"
edition = "2024"

[dependencies]
nomos-core = { path = ".." }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1.38", features = ["full"] }
tracing-subscriber = "0.3"
